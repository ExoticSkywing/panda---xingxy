# 邀请任务视觉增强补丁

## 修改日期
2026-01-28

## 功能说明
通过 CSS/JS 注入方式增强"邀请好友注册"任务的视觉效果，提高用户关注度。

## 增强效果
1. **高亮背景**：紫色渐变背景使邀请任务从列表中脱颖而出
2. **热门标签**：右上角显示"🔥 热门"标签
3. **复制链接按钮**：一键复制推荐链接，降低参与门槛
4. **悬停动效**：鼠标悬停时有上浮和发光效果

## 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `assets/css/referral.css` | 新增 | 视觉增强样式 |
| `assets/js/referral.js` | 新增 | 功能增强脚本（复制链接等） |
| `inc/assets.php` | 新增 | 资源加载模块 |
| `init.php` | 修改 | 加载 assets.php |

## 技术实现

### 自动识别机制
JS 脚本通过文本匹配识别包含"邀请好友注册"的列表项，自动添加：
- `.xingxy-referral-highlight` 样式类
- 热门标签 DOM 元素
- 复制链接按钮

### 推荐链接获取
```javascript
// 从 PHP 传递到前端
wp_localize_script('xingxy-referral', 'xingxy_referral', array(
    'user_id' => $user_id,
    'referral_url' => add_query_arg('ref', $user_id, home_url('/')),
));
```

## 恢复方法
```bash
cd /www/wwwroot/xingxy.manyuzo.com/wp-content/themes/panda/xingxy
git revert HEAD
```

## 依赖
- jQuery（主题已内置）
- Font Awesome 图标（主题已内置）
