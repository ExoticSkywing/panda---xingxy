# 推广任务视觉增强 (Referral Visual Enhancement)

## 🎯 目标 (Objective)
将 "邀请好友注册" 任务卡片从默认样式改造为 **高定级(High-End)** 视觉体验。
特别是针对 Panda 主题的夜间模式进行深度适配，实现 "大气、融合、生动" 的效果。

## 🎨 视觉方案 (Final Design: Scheme N)

### 1. 核心视觉 (Core Visuals)
*   **流体背景 (Fluid Bubbles)**:
    *   **Day**: 清新水彩 (Pastel)，微弱流动。
    *   **Night**: **灰紫星云 (Grey-Purple Nebula)**，深邃且高饱和度混合 (Hard-Light)。
    *   **Animation**: Turbo Speed (10s-20s)，高动态流动效果。
*   **极光文字 (Aurora Text)**:
    *   夜间模式下，邀请文案表面有 **浅金/浅紫流光** 缓缓扫过 (Background Clip Text)。
    *   JS 强制剥离了主题默认的灰色样式，确保 100% 可见度。
*   **Glass Button Pro**:
    *   全套移植毛玻璃按钮，带有 **光流边框 (Conic Gradient)** 和 **内部辉光**。
    *   交互时有缩放和阴影下沉反馈。

### 2. 布局重构 (Layout Refactor)
*   **左侧**: 🎁 高清 SVG 悬浮图标 (60px+)，带浮动动画。
*   **中间**: 标题 + 极光流光文案。
*   **右侧**: 积分数字 + 两个操作按钮 (垂直排列 -> 形成操作闭环)。
*   **标签**: 贴边实心渐变红 "福利" 标签。

## 🛠️ 文件变更 (File Changes)

### 1. `assets/js/referral.js`
*   **逻辑重写**:
    *   使用 `Template String` (反引号) 注入复杂的 SVG 代码。
    *   **[CRITICAL]** 强制移除 `.muted-color` 类名，防止主题样式覆盖文案颜色。
    *   动态注入 Background DOM 和 Glass Button DOM。
    *   监听 `MutationObserver` 确保动态加载内容也能生效。

### 2. `assets/css/referral.css`
*   **CSS 变量架构**: 定义 `--xingxy-card-bg`, `--b-color`, `--angle-1` 等变量，实现深浅模式自动切换。
*   **关键动画**:
    *   `moveInCircle`, `moveVertical` (气泡流动)
    *   `text-shine` (文字流光)
    *   `border-rotate` (按钮光边)
*   **高可见度**: 强制定义 `.xingxy-referral-desc` 在夜间模式为纯白/流光。

## ⚠️ 恢复与回滚 (Recovery)
如果视觉出现异常（如全白、错位），请执行：
```bash
# 1. 并没有删除任何核心文件，只需还原 JS/CSS
git checkout HEAD~1 assets/js/referral.js assets/css/referral.css

# 2. 如果 JS 报错 (如 Unexpected token)，检查 SVG 字符串是否正确使用了反引号包裹
```

## 📝 版本历史 (History)
- **v1.0**: 简单的 Highlight 边框 (Rejected)
- **v2.0**: 静态渐变背景 (Rejected)
- **v3.0**: 流体气泡 + 普通按钮
- **v4.0 (Final)**: 
    - 集成 Glass Button
    - 布局重构 (Layout Refactor)
    - 灰紫星云背景 (Grey-Purple)
    - 极光文字 (Aurora Text)
    - 动画加速 (Turbo Speed)
